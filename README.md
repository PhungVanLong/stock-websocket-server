# 📈 Stock WebSocket Bridge

WebSocket server cho phép stream dữ liệu chứng khoán Việt Nam theo thời gian thực.

## 🚀 Cách chạy local
```bash
pip install -r requirements.txt
python server.py
